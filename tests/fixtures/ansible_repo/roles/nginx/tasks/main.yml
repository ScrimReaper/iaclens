---
- name: Install nginx
  apt:
    name: nginx
    state: present
  notify: restart nginx

- name: Start nginx
  service:
    name: nginx
    state: started
  notify:
    - restart nginx
    - nginx-listeners
